{
  "name": "@peer-bridge/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Daemon: P2P transport, room state, inbox, local IPC server",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test src/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo ok"
  },
  "dependencies": {
    "@peer-bridge/core": "workspace:*"
  }
}
