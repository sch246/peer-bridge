{
  "name": "@peer-bridge/rendezvous",
  "version": "0.1.0",
  "private": true,
  "description": "Rendezvous server: signaling relay, invite pairing, federation",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test test/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "echo ok"
  },
  "dependencies": {
    "@peer-bridge/protocol": "workspace:*",
    "fastify": "^5.3.3",
    "@fastify/websocket": "^11.0.3",
    "libsodium-wrappers": "^0.8.4",
    "smol-toml": "^1.3.4"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3",
    "ws": "^8.18.0"
  }
}
