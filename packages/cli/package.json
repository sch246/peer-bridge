{
  "name": "@peer-bridge/cli",
  "version": "0.1.0",
  "private": true,
  "description": "CLI: standalone P2P file transfer and daemon management",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "peer-bridge": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "node --import tsx --test \"src/**/*.test.ts\"",
    "typecheck": "tsc --noEmit",
    "lint": "echo ok"
  },
  "dependencies": {
    "@peer-bridge/core": "workspace:*",
    "@peer-bridge/protocol": "workspace:*",
    "smol-toml": "^1.3.4"
  },
  "devDependencies": {
    "tsx": "^4.22.3",
    "@types/node": "^25.9.1",
    "typescript": "^6.0.3"
  }
}
